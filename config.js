const SECRET_KEY = "R4h4s14";
const PORT = 5000;
const HOST = "localhost";
const BASE_URL = `http://${HOST}:${PORT}`;

const DB_HOST = "localhost";
const DB_USER = "postgres";
const DB_PASSWORD = "aini";
const DB_NAME = "RecipeVault";
const DB_PORT = 5432;

module.exports = {
  SECRET_KEY,
  PORT,
  HOST,
  BASE_URL,
  DB_HOST,
  DB_USER,
  DB_PASSWORD,
  DB_NAME,
  DB_PORT,
};
